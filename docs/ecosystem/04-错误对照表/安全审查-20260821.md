# 安全审查记录（2026-08-21，v3.8.30 安全收尾）

审查范围：GitHub 公开仓库全部内容（当前工作区 + git 历史）+ CI 产物配置。

## 审查结论（已确认干净）

| 检查项 | 结果 |
|---|---|
| git 历史 token/密钥 | 全部为占位符（ghp_your_*），无真实 PAT |
| 代码硬编码密钥 | 无（密钥全走运行时 Settings 配置） |
| google-services.json | 占位文件（rincore-dummy / dummy key），非真实配置 |
| 文档/skills/AGENTS/CLAUDE | 无 token/密钥/私人邮箱/手机号/坐标/内网 IP |
| 定位相关 | get_location 实时定位优先，无硬编码兜底坐标 |
| local.properties / .env / p12 | 从未入库 |
| 作者信息 | main 已统一为 desginus（filter-branch），Contributors 唯一 |

## 处置（高优项）

1. **app/release.jks 签名密钥入库 + 密码公开（高危，已根治）**
   - 仓库内 jks 已从工作区、索引与全部 git 历史中移除（filter-branch --index-filter，main 全链）
   - tag 重指：v3.7.0 / v3.8.19 / nightly 已指向清理后的新链；fork 前上游 tag（0.0.x/2.4.x 等 171 个）为原版快照且不含 jks，原样保留
   - 新签名密钥已生成并存入 GitHub Actions Secrets（KEYSTORE_BASE64 / KEYSTORE_PASSWORD，2026-08-21 创建）
   - 本地副本：/workspace/rincore-keystore/（环境外不可恢复，已提示用户本地备份）
   - workflow（build.yml / daily-build.yml）改为从 Secrets 注入，secrets 缺失时跳过签名构建并告警
   - 影响：新签名不同，旧 APK 无法覆盖升级，需卸载重装一次（用户决定：开源不迁就升级便捷，信息安全优先）
2. 历史备份：/workspace/rincore-backup-20260821.bundle（清理前）、/workspace/rincore-backup-20260821b.bundle（最终状态）

## 后续注意

- 任何密钥/令牌绝不写入仓库（含文档、skills、commit message）
- google-services.json 保持占位（真实 Firebase 配置仅本机构建使用，不入库）
- 发版后核对 CI 签名是否使用 Secrets（日志可见 signing config 生效）
