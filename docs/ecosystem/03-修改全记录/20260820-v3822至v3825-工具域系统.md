# v3.8.22-25 工具域系统修复系列（2026-08-20）

参考声明：本文档是参考不是教条，记录的是当时事实，可能过时。以实际代码行为准。

## v3.8.22 压缩位点重启丢失 + 抽屉删助手
- ConversationEntity 加 compress_retentions 列（Room v27→v28 Migration_27_28），双向 JSON，存量无损
- 抽屉更多页删除"助手"入口，其余顺位替补、设置不动

## v3.8.23 工具域四修
- 对照页真校验（域内合计 vs 池总数→完全一致/请对照bug）
- exemptFromDomainTools 持久化缺失（B33，Settings 有字段无 PreferencesKey）
- 自定义空域保留进 unifiedDomainView.tree（新建域 invoke_tools 可寻址）
- 移动目标列表路径排序 + 内置空壳剔除

## v3.8.24 统一信息源头 + 卡顿根治
- ToolListPage/SettingDomainPage 移动目标、筛选 chips 全部改用 unifiedDomainView.tree（废除自拼 entries+customDomains）
- 预构建工具名→域映射一次，点击筛选零重分类

## v3.8.25 管理子域数量 0 修复
- 根因：管理子域对话框 customSubs 取 name 短名（非 normalizedFullPath）→ classified 完整路径查表落空；删除匹配同失效
- 修复：allSubs 用 unifiedView.tree[parentDomain] 同源；isCustom/删除匹配 normalizedFullPath
- 铁律延伸：域展示/操作一律完整路径 + 统一视图，禁用 name 短名自拼
