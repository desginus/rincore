# 2026-08-11 v3.6.17 — 深度功耗优化 + 其他功能入口挪底

## 1. 入口调整 (用户)
- 原「高级功能」入口: 顶部 → 设置页最底部
- 改名「其他功能」, 描述改为「更多设置项」 (低调不显眼)
- 新页标题同步改名

## 2. 深度功耗优化 (不改参数, 纯代码)
- release 裁剪 debug/verbose 日志 (proguard-rules):
  - 每 chunk SSE 日志 (onEvent data 大 JSON 格式化) 是流式热点
  - 裁剪后 release 不再打 d/v 级日志 → CPU/IO 大降
- 请求体日志 Log.i → Log.d (3 Provider):
  - 每请求全请求体 JSON 序列化+格式化 (长对话大字符串) 是热点
  - debug 保留诊断, release 裁剪
- 保留: Log.i/w/e (cache-fp/中断/错误 — 诊断关键日志)

## 3. 审查结论 (无后台轮询/无主线程 IO/无大缓存)
- while(true) 均为正常分页/计算/解压循环
- saveConversation 走 suspend Room (IO 线程)
- 冷启动预热已在; 断流重试/空流重试/watchdog 已在
