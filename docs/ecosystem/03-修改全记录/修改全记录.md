# 修改全记录

> 参考声明：本文档是参考不是教条，记录的是当时事实，可能过时。以实际代码行为为准。

更新日期：2026-08-06
覆盖范围：v2.9.4 → v3.5.17 全部版本改动，含文件级变更、引入原因、回滚记录。
与 rincore-changelog skill 同步，本文件为完整版。

## 版本链总览

```
v2.9.4 (97faaa9a) → v2.9.5 (43cf1b47) → v2.9.6 (21b95614) → v2.9.7 (9ec8629e)
→ v2.9.8 (c0280099) → v3.0.x (压缩系统期) → v3.1.0 (583a38c1, 连接问题引入点)
→ v3.2.x (动态工具/域分类) → v3.3.x (稳定性/缓存) → v3.4.x (传输层补丁期)
→ v3.5.0 (传输层回滚) → v3.5.1~3.5.17 (连接根治/缓存稳定期, 当前)
```

## 历史教训（防重踩，改动前必读）

1. limitContext 滞回策略 ↔ 缓存：v3.3.0 引入 → v3.3.5 回滚（缓存机制报废）→ v3.3.12 确认。函数在 Message.kt 但未启用。勿重新启用
2. 缓存锚点/注入隔离：v2.9.5 注入隔离引入 SETTINGS 协议违规 → v3.4.5 修复。协议合规 > 缓存边际收益
3. DeepSeek Responses reasoning：3.5.4~3.5.6 猜测性修复全废 → 3.5.7 按官方协议 → 3.5.8 工具轮相邻消息 → 3.5.9 起搁置
4. 工具执行无超时：3.5.9 withTimeout 60s 兜底
5. 缓存卡-跳-线性：DeepSeek 服务端磁盘缓存机制，客户端不可控（decisions D2）
6. v3.1.0 引入连接问题：三 Provider 静默恢复 + HTTP/2 优先 + 连接配置改动，是 3.x 中断问题的根源版本
7. v2.9.8 是连接行为正确参照：SSE 未收到数据自动重试（指数退避），收到数据后中断传播异常
8. 请求体零改动：任何 system/tools/域概览变化破坏缓存前缀

## v3.5.x 详细记录

### v3.5.17（当前，2026-08-05 起，连接根治系列）

阶段一：缓存回滚（1784ba75）
- 回滚 3.5.16 请求侧改动：use_skill 移出框架工具集（tools 数组恢复）、UNCLASSIFIED 域移除（layer1 恢复）
- 原因：3.5.16 改动请求体导致缓存率暴跌（用户实测）
- 保留：P0 修复（子请求 system 兜底不影响主请求）、弹窗细化、Trace ID、计数、list_domains

阶段二：连接层回滚（6161dd99 → 0a229541）
- readTimeout 恢复又按用户指示定回 3min
- SSE 无数据看门狗取消主动断开，只记录日志
- 原因：3.5.14 引入的主动断开机制是中断元凶

阶段三：静默恢复移除 + content 数组解析（1f9d350e → 60389917）
- ChatCompletionsAPI/ResponseAPI/ClaudeProvider/GoogleProvider 全部移除 hasData 静默 close
- 根因：v3.1.0 引入的三 Provider 静默恢复，中断被吞无报错
- parseMessage content 数组拼接对齐原版（数组格式 content 不再丢失）

阶段四：v2.9.8 SSE 重试机制移植（98f5d7d6）
- hasReceivedData/retryCount/maxRetries/currentEventSource/connect() 完整移植
- 未收到数据自动重试，收到数据后中断传播异常
- 这是 2.x 稳定期的关键机制，3.5.0 回滚时丢失

阶段五：MCP 状态撕裂修复（7ecda960）
- addClient 的 getTransport 包 runCatching（stdio 失败不再中途退出）
- getAllAvailableTools 过滤 Error 状态服务器
- callTool 报错明确化
- 经验文档固化：B18/B19/缓存教训写入 rincore-bug-record

阶段六：完全禁用 HTTP/2（b27253cd → 895725c4）
- protocols 只留 HTTP_1_1
- 根因：DeepSeek 服务端 ALPN 协商到 h2 后报 stream was reset: PROTOCOL_ERROR
- protocols(HTTP_1_1, HTTP_2) 顺序不影响 ALPN，服务端支持 h2 必选 h2

阶段七：计时器/灵动岛收尾（f9ea683a → 520b4cb0）
- onCompletion 收尾包 NonCancellable（取消态也执行落盘 + 生成结束事件）
- stopGeneration 显式 tryEmit ChatGenerationEnded
- 根因：job.cancel() 后 onCompletion 挂起调用跳过，灵动岛不取消、计时器不停

阶段八：连接配置对齐 v2.9.8 + keepalive 60s（75c8e595）
- writeTimeout 120s、ConnectionPool(12, 60s)、pingInterval 30s
- 根因：工具执行 60s+ 后连接空闲超服务端关闭时间，复用陈旧连接 EOF
- SSE 重试 3→5 次（指数退避 31s 窗口）

### v3.5.16（55a46243，缺陷报告 v5 全部 7 项）
- P0-1 子请求首条 system 兜底（OpenAIProvider.generateText 统一处理）；Trace ID 随机 UUID；CallTracer 计数修正
- P0-2 错误弹窗细化（ErrorCard 展开详情 + 复制）
- P1-1 MCP 状态文本
- P1-2 UNCLASSIFIED 兜底域（后回滚）
- P2-1 思考链计时兜底
- P2-2 list_domains 移除残留
- P3-1 use_skill 入框架工具集（后回滚）

### v3.5.15（2acaef9c）
- MCP STDIO 导入解析（parseMcpServersFromJson 补 stdio 分支）
- HTTP/1.1 优先（protocols HTTP_2,HTTP_1_1 → HTTP_1_1,HTTP_2）

### v3.5.14（a4b73fc0 起）
- ResponseAPI 断线恢复、SSE 看门狗 120s、readTimeout 3min
- DeepSeek effort 映射、MCP STDIO 实现、生成错误上下文日志

### v3.5.13~3.5.7（细节见 changelog skill）
- v3.5.13 删除模型级联清理
- v3.5.12 热力图崩溃修复
- v3.5.11 移植 SystemPromptBuilder（缓存正常化基准）
- v3.5.10 msg_fp 消息指纹日志
- v3.5.9 工具执行 60s 超时
- v3.5.8 工具轮相邻 assistant 消息
- v3.5.7 明文 content 修复

### v3.5.4 ~ v3.5.6（废弃）
- reasoning 猜测性修复全部无效，回滚到 3.5.3 纯基线

### v3.5.3（干净基线）
- 70K 冷启动注入根治（MCP 懒加载，65K→6K）
- MCP 动态工具并入域名池经 invoke_tools 加载

### v3.5.0（传输层回滚）
- checkout 3.2.2 八文件：OpenAIProvider/ChatCompletionsAPI/ResponseAPI/Message/GenerationHandler/PlaceholderTransformer/PromptInjectionTransformer/ChatService
- 用户决策：补丁式修复无法解决连接中断
- 保留：流式落盘/保活/权限/闹钟/FGS/DI

## v2.x 关键版本（连接行为参照）

- v2.9.8（c0280099）：SSE 重试机制（未收到数据重试 3 次指数退避）——连接稳定正确参照
- v2.9.5（43cf1b47）：缓存锚点 + 注入隔离（引入 SETTINGS 协议违规）
- v2.9.4（97faaa9a）：工具 systemPrompt 瘦身（框架工具只注入）

## 已知但未修问题

1. 传输层 G1-G4 缺口：G1 BEFORE_SYSTEM_PROMPT 协议违规（P0）、G2 孤立 tool_call 无清理、G3 平台空流未处理、G4 缓存破坏诊断缺失
2. 缓存阶梯式：DeepSeek 服务端机制，客户端不可控（decisions D2，不修）
3. F2 workspace_shell 进程无法持久化：proot 沙箱机制，代码不可修复
