# v3.9.11 ~ v3.9.13 修改全记录 (2026-08-23)

## v3.9.11 — 用户反馈 6 项批量修复 (提交 86f06dcc / d260b3ca / e5ce1a6e)
1. MCP Idle 图标: Clock02 回原版 MessageBlocked (撤销误改), 文案保持已配置待连接
2. PDF 放大闪烁: PdfRenderView 位图双缓冲 — zoom 变化不销毁旧位图, 后台渲染新分辨率完成后替换
3. DOCX 字体间距: buildDocxHtml 全面重写 —
   - rPr 解析 sz/rFonts/b/i/color, run 级 span style 包裹
   - spacing after/before/line + lineRule (auto 倍数 / exact·atLeast pt)
   - styles.xml docDefaults 全局默认字号/行距/段后距 (parseDocxDefaults)
   - 段落级 margin/line-height/text-align 还原
4. PPT 空白根因: 1280x720 固定容器在 360dp viewport 溢出截断 → 外层 overflow:auto 横向滚动包装
5. UsagePage 焦点视图判定: focusMode 走 UsageRingCard 4 卡竖列大窗 (与单密钥一致); 切换按钮文字改图标 (多卡片=FileView, 焦点=View), 持久化 usageViewMode 已有
6. ApiKeyQuickSwitcher 重启丢失: 重写自管理持久化 — persistKeys/persistApiKey 直接 scope.launch settingsStore.update 写 settings.providers 对应项 (copyProvider 透传 savedKeys / sealed when 分支 copy(apiKey=)), 不再依赖页面保存按钮

## v3.9.12 — 原版 2.4.11 全量深度移植 (提交 aedacb3c, 17 文件 +761 行)
对应原版 2.4.10..2.4.11 (b270766, 15 提交, 36 文件):
1. 网络设置 (新功能):
   - Settings.networkSetting 数据类 + PreferencesStore NETWORK_SETTING 键序列化
   - 新建 data/network/ProxyConfig.kt: toProxyOrNull + SettingsProxySelector / SettingsProxyAuthenticator / SettingsSocks5Authenticator (94 行原版)
   - DataSourceModule: proxySelector + proxyAuthenticator + SOCKS5 全局注册; 代理参数 AtomicReference 对比变更 → evictAll 热重连; 保留 RinCore 连接基线 (HTTP/1.1 only / readTimeout 3min / keepalive 60s 不变)
   - 新建 SettingPreferencesNetworkPage (UA + 代理弹窗 + 连接测试 google generate_204)
   - SettingPreferencesPage 网络入口 + RouteActivity 注册 Screen.SettingPreferencesNetwork
   - RequestLoggingInterceptor 脱敏 Proxy-Authorization
2. 本地备份选择+覆盖确认: BackupVM.localBackupItems StateFlow; exportToFile / restoreFromLocalFile 按选择 items; ImportExportTab 顶部 MultiChoiceSegmentedButtonRow (聊天记录/文件); 导入前 AlertDialog 覆盖确认; BackupPage Tab 重排 (本地导入导出提前到第一位)
3. DeepSeek V4 Flash Vision: ModelRegistry 新 DEEPSEEK_V4_FLASH_VISION_EXP (vision)
4. ModelRegistry contextLength: ModelDsl 加字段+Builder 函数; MODEL_CONTEXT_LENGTH; Int.k / Int.m 扩展; DEEPSEEK_V4_FLASH / PRO 注册 1.m
5. ThinkTagTransformer 重写: THINKING_REGEX 仅匹配行首; 已有 Reasoning 不再二次创建; 抽 transformThinkTags(generationFinished); onGenerationFinish 补 finishedAt
6. 小米 MiMo thinking: ChatCompletionsAPI 加 api.xiaomimimo.com / token-plan-cn.xiaomimimo.com
7. 跳过项 (有原因): AssistantBasicPage 数字输入 (项目无 contextMessageLimit 字段); FullScreen→Fullscreen (库版本不同步); UpdateChecker StateFlow (已禁用更新检查); 模型图标 SVG (视觉迭代)

## v3.9.13 — 两次小迭代 (提交 290cde54 → revert 70a0eed8, 再 d7f28ece)
1. 第一次 (290cde54): 设置主页增加网络设置直达入口 — 用户要求作废, 已 revert
2. 第二次 (d7f28ece): User-Agent 默认行为纯净化 —
   - 移除 RikkaHub-Android/版本 默认 UA 发送逻辑, 留空完全不发自定义 UA (OkHttp 默认兜底)
   - NetworkPage 删除 defaultUserAgent 变量/占位/desc %s 拼接, 新增 placeholder 文案
   - 文件头注释清理 RikkaHub 字样
   原因: 用户不希望向外部暴露客户端标识, 避免污染请求头/上下文

## 待办
- 澎湃 OS4 (HyperOS 4, Android 17 底层) 适配准备
- 骁龙 8 Elite (8 Gen 4) 性能优化