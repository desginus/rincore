# 2026-08-09 v3.6.9 — 子域计数 + 联网图标删除 + 统计实时 + 额度刷新

## 1. 子域工具计数修复 (用户: 工具在子域 → 根域统计显示 0)
- 根域计数改为 subtreeCounts (直接 + 全部子域合计)
- 子域计数同样 subtree (含更深层子域)
- 帮助文本已有"含子域共 Y 个"标注 — 现 UI 一致

## 2. 输入栏联网图标删除 (用户: UI 更整齐)
- ChatInput 移除 SearchPickerButton 图标 + enableSearch/onToggleSearch 参数
- enableWebSearch 设置保留 (设置页控制)

## 3. 消息底部统计实时性 (用户: Token/缓存隔半天才更新)
- include_usage 已开 (v3.5.x) — 但部分平台只在流结束发 usage
- 新增: 流式期间按增量文本实时估算 completion tokens (O(1) 增量,
  断流回滚时重置, 结束被真实 usage 覆盖)
- 输入 token/缓存数: 平台提供时实时 (首个 usage chunk), 否则结束显示真实值

## 4. 额度显示脉冲式修复
- 缓存 TTL 2min → 10min (额度变化慢, 减少频繁重拉)
- 拉取失败保留旧值 (不闪 Error)

## 5. 缓存前缀对齐确认 (对照 v3.5.34)
- stableSystem = 锚点(含模型名 v3.6.6) + systemPrompt + layer1(配置驱动) — 静态
- tools 数组确定性排序 (v3.5.58) + 分层注入 (框架+已加载域)
- 与 v3.5.34 缓存口径一致 (v3.5.52 对齐 + 无破坏性变化)
- cache-fp 指纹日志可验证: 连续多轮一致 = 前缀稳定

## 待确认
- Mimo 平台归属 (用户列出 MiniMax/Mimo — 需确认 Mimo 是哪家开放平台)
- 阿里云百炼/智谱/MiniMax/Mimo 无公开余额 API (仅 DeepSeek/月之暗面/
  OpenRouter 有) — 无法通过 API 读取, 需平台侧提供
