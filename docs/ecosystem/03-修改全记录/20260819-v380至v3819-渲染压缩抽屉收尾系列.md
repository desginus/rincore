# v3.8.0 → v3.8.19（用量页/流式稳定性/渲染/压缩/抽屉收尾系列，2026-08-18~19）

参考声明：本文档是参考不是教条，记录的是当时事实，可能过时。以实际代码行为准。

## 版本链

v3.8.0-8.2（用量页+缓存断根）→ v3.8.3-8.8（OpenCode 流式/断流/SSE 诊断）→ v3.8.9-8.11（用量页格式+液态玻璃）→ v3.8.12-8.13（渲染抽动修复）→ v3.8.14（压缩重做）→ v3.8.15-8.18（抽屉块状+清理）→ v3.8.19（压缩 UI 收尾+文档）

## v3.8.3-8.8 OpenCode 中转流式优化（用户实测迭代）
- v3.8.3：UI 节流 100→50ms + ClaudeProvider watchdog（opencode 首包 120s/流中 180s，hasData AtomicBoolean）
- v3.8.4：toHttpUrl import + watchdog launch receiver（编译修）
- v3.8.5：message_stop 断流检测（completed 标记，无 message_stop 关闭=断流）
- v3.8.6：节流 5ms（用户指令）+ SSE 诊断日志（TraceLogger.takeTagged + sse_diag metrics）
- v3.8.7：节流回 50ms（5ms 实测掉帧式顿挫）
- v3.8.8：断流重试 7 次 5 秒内（200/400/800 + 900x4）+ 用量页剩余时间

## v3.8.9-8.11 用量页格式 + 液态玻璃
- 用量页剩余时间进位（天/周）+ 重置倒计时精确时间（12 小时制 + 8 时段：中午 11-14/下午 14-17:30/傍晚 17:30-19/夜晚 19-23:30/深夜 23:30-3/凌晨 3-6/清晨 6-8/早晨 8-11:30）
- 液态玻璃分享后丢失：ON_RESUME hazeRebuildTick + key 重建背景纹理
- 教训：v3.8.11 提交漏改 versionName（净版本跳号）；"import androidx.lifecycle.Lifecycle" 是 LifecycleEventObserver 前缀导致 not-in 判断恒假、import 两次漏写

## v3.8.12-8.13 输出完成抽动修复
- 根因：v3.7.x 条件化 animateContentSize，loading 翻转改修饰符链 → 重组合 + 重渲染 + 高度动画
- 修复：always animateContentSize + 参数化（TweenSpec(0)/SpringSpec），注意 tween/spring 为 @Composable 不可用于顶层属性、helper 插入勿吃 @Composable 注解、UIMessagePart 包为 me.rerere.ai.ui

## v3.8.14 压缩机制重做
- CompressRetention（id/时间戳/原文/摘要），最多 3 位点覆盖最旧；restoreCompressAt 级联撤销；migrateLegacyCompress 兼容旧 compressedContext
- UI：更多页"上下文压缩管理"弹窗（位点列表 + 查看/恢复）

## v3.8.15-8.18 抽屉块状 UI + 清理聊天内容
- DrawerActions 块状三入口（标题"聊天历史"+ 搜索/查询/清理），清理对话框三档（3 个月/1 个月/1 周前）
- 跳过规则：置顶（is_pinned）或含收藏（favorites 表 node:<convId>: 前缀）；DAO getCleanupCandidates；删除复用 ConversationRepository.deleteConversation
- 编译修：SpringSpec/TweenSpec 类构造、@Composable 归位、UIMessagePart 包名、RoundedCornerShape import

## v3.8.19 压缩 UI 收尾
- 位点条目条状窄 UI（单行：时间戳 + 查看/恢复紧凑按钮）
- 撤销按钮去红色（surfaceContainerLow + onSurfaceVariant 图标）改名"上下文压缩管理"
- 查看相关信息显示压缩摘要（summaryMessageNodes），不再累计原文（原文仍留存供恢复）
- 文档收尾 + tag
