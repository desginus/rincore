# v3.6.101 → v3.6.123 修改全记录（2026-08-16 深夜至 08-17 凌晨）

## 阶段一：对齐标注与常规优化（v3.6.101-103）
- 全量原版对齐标注（767 文件四类清单 + 专项标注）
- 工具改名能力（toolNameOverrides，MCP 页 + 工具页）
- callTool 内部 60s 超时兜底
- ClawPluginRegistry 重命名（消除双 PluginManager 冲突）

## 阶段二：插件系统攻坚（v3.6.104-119）
- 方法域改未分类、Skills 搜索、设置页重排
- 强制启动（forcedSkills 注入 system 尾部）
- 插件与技能隔开：plugin__ 工具 + 插件域
- 插件页可见性：plugin.json 元数据 + .claude-plugin 支持
- 插件真实名目录、删除能力、.mcp.json 落盘
- invoke_tools 实时域（toolPoolProvider）

## 阶段三：MCP 事故与回滚（v3.6.120-123）
- v3.6.112 的插件自动桥接破坏 MCP 连接，用户实测大部分 MCP 无法连接
- v3.6.120 移除自动桥接调用点
- v3.6.121 启动清理 plugin__ 残留（settings + 白名单）
- v3.6.123 编译修复收尾

## 关键教训
1. 新增自动 MCP 注册必须评估对既有连接的影响，禁止启动路径批量 addClient
2. 修复"写数据"类 bug 必须验证调用链存在（installFromParsed 死代码教训）
3. 每次提交前跑静态校验脚本（brace 平衡/引用存在性/历史编译易错点）
4. 插件系统最终形态：安装落盘插件目录 + 插件页显示/删除 + plugin__ 技能工具（纯工具）+ MCP 手动连接
