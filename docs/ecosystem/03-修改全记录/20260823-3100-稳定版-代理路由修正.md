# v3.10.0 稳定版 (2026-08-23)

用户指令: 重新推稳定版本, 能改的都优化, 不留 bug; 版本 3.10.0。

## 改动
1. 模型勾选弹窗滚动: heightIn(max=480.dp) + verticalScroll — 原静态卡死
2. 代理路由逻辑修正: 移除 default === defaultClient 判定 —
   Claude 独立连接池请求原先永远无法走代理 (bug), 现在勾选即代理
3. 持久化链路复核通过: SettingVM.updateSettings → SettingsStore.update →
   DataStore.edit 落盘, 开关/勾选重启不丢
4. 全链路自检: name 字段存在, 测试按钮受总开关约束, 空代理地址
   安全回退 NO_PROXY, 默认 client 已剥离代理
5. README 徽章 / changelog skill / .agents 同步

## 前置版本批注 (v3.9.12 ~ v3.9.15)
- v3.9.12 原版 2.4.11 全量移植 (网络设置/备份选择/vision/contextLength/MiMo)
- v3.9.13 UA 纯净 (撤 RikkaHub 标识)
- v3.9.14 澎湃OS4/骁龙8E 设备环境检测, 16KB 对齐核查达标
- v3.9.15 代理开关 + 部分开启按模型勾选 (ProxyRoute)
