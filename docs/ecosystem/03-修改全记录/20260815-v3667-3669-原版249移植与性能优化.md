# 2026-08-15 v3.6.67-v3.6.69 原版 2.4.9 移植与性能优化

## 版本链

| 版本 | 内容 | 提交 |
|------|------|------|
| v3.6.67 | 清理诊断显示 + 移植原版 2.4.9 三处修复 | c5635397 |
| v3.6.68 | 移植首页输入栏模糊 (haze 升级) | 65dce1be |
| v3.6.69 | 消息页渲染深度优化 + 截断 3 条 | 48d4bf0a |

## 一、清理诊断显示 (v3.6.67)

删除缓存排查期临时加的 UI 诊断显示，回归干净界面：

- HeadroomStats.lastMsgFp（消息指纹）删除
- HeadroomStats.lastDeltaInfo（增量诊断）删除
- GenerationHandler 的 msg_fp 计算与赋值删除
- ChatMessageNerdLine 的"指纹"“增量"两行显示删除

## 二、移植原版 2.4.9 三处修复 (v3.6.67)

来源：upstream-try (rikkahub/rikkahub) 2.4.9 标签，逐提交对照移植。

### 1. reasoning max effort（edbd5773）

- ReasoningLevel 新增 MAX(32_000, "max") 档位
- ChatCompletionsAPI：api.deepseek.com 映射 XHIGH/MAX → reasoning_effort "max"；integrate.api.nvidia.com 同步
- ReasoningPicker：MAX 档图标（ReasoningHigh）、刻度标签（labeledLevels 只显示 OFF/AUTO/LOW/MAX）、label() 加 reasoning_max
- strings.xml 中英文加 reasoning_max（Max / 最大）

### 2. 图片生成 Provider Override（9b964ca0）

ImgGenVM 此前 findProvider 之后又按 id 回查全局 provider 列表，模型上的 override 无法解析。改为直接用 findProvider 结果，generateImage/editImage 传 provider，与聊天生成一致。

### 3. null tool_calls 崩溃修复（f55c8c14）

部分 OpenAI 兼容接口把 delta/message 显式写成 null，强制解析中断整轮生成。ChatCompletionsAPI 流式解码改为：delta/message 为 null 时跳过该 chunk，不再 throw。

### 附：jlatexmath R8 修复（9adc82ec）

上游把 keep 规则从 cyrillic/greek 单类改为 `-keep class org.scilab.forge.jlatexmath.** {*;}`。RinCore 的 app/proguard-rules.pro 26 行已是修复后写法，无需移植。

## 三、首页输入栏模糊 (v3.6.68)

对齐原版 2.4.9 的 haze 模糊升级（上游 c077e0c8 依赖更新 + ChatInput API 迁移）：

- haze 2.0.0-alpha03 → 2.0.0-alpha05
- haze-blur-materials → haze-blur-material3
- ChatInput 输入栏：HazeMaterials.thin → HazeBlurStyle.Material3（blurRadius 12dp）
- hazeEffect/blurEffect → hazeBlur + HazeInput.Sources(hazeState)

输入栏获得原版同款 Material3 模糊质感。

## 四、消息页渲染深度优化 (v3.6.69)

针对用户反馈"上下滑动特别卡、不跟手、阶段性卡死 60Hz"，三处渲染优化：

### 1. LazyColumn 加 contentType

ChatList 的 itemsIndexed 此前无 contentType，所有消息条目视为同类型，滚动时无法按类型复用。加 contentType = currentMessage.role，按角色分类型缓存。

### 2. MarkdownBlock 初次解析异步化

根因：MarkdownBlock 首帧 `remember { mutableStateOf(parseMarkdown(content)) }` 在主线程同步解析 Markdown AST。LazyColumn 滑动回收重建条目时反复同步解析长消息，阻塞主线程，是滑动卡顿不跟手的直接原因。

修复：初始 data 为 null（空占位），LaunchedEffect + snapshotFlow + flowOn(Dispatchers.Default) 后台解析后填充。主线程不再被 Markdown 解析阻塞。

### 3. 流式 UI 节流 50ms → 100ms

流式期间重组频率从每秒最多 20 次降到 10 次，进一步降低流式输出期间的掉帧与滑动卡顿。

## 五、上下文截断 3 条 (v3.6.69)

用户要求：纯滞回截断保留条数 30 → 3。只保留最近 3 条消息（截断点往前调整到 USER 消息边界，避免拆开 tool 配对）。

## 验证

- CI 绿（v3.6.67 / v3.6.68 / v3.6.69 三个 run 均 success）
- 待用户实测：输入栏模糊质感、思考强度最大档、滑动流畅度、120Hz 稳定性

## 六、v3.6.70 思考深度 UI + 降维改名 + 模糊减负（0a5a08a8）

### 思考深度 UI

- 刻度全部 7 档显示标注：关闭 / 自动 / 低 / 中等 / 较高 / 高 / 最高
- 标签修正：HIGH → 较高、XHIGH → 高、MAX → 最高（英文 High / Very High / Max）
- 最高档（MAX）灯泡呼吸闪烁（alpha 动画 650ms），加提示文字"灯泡闪烁表示已达到最高思考强度"

### 上下文降维改名

- "上下文降维" → "节选最近对话"
- 描述：开启后发送上下文时只保留最近 3 条消息，更早的历史不发送；关闭（默认）为完整模式，所有消息原样发送
- 设置页状态文案同步："已开启 · 只发送最近 3 条消息" / "已关闭 · 完整模式（消息原样发送）"
- 消息统计行"降维省" → "节选省"

### 刷新率减负

- 输入栏模糊半径 12dp → 8dp，降低 120Hz 下每帧 GPU 模糊采样开销
- 用户反馈现象（120Hz 下卡、60Hz 下反而流畅）说明是帧预算问题：120Hz 每帧 8.3ms，渲染负载超预算即掉帧。持续按此方向减负。
