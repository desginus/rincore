# 迭代框架

> 参考声明：本文档是参考不是教条，记录的是当时事实，可能过时。以实际代码行为为准。

更新日期：2026-08-23（当前版本 v3.9.2，单产品线）
用途：每次迭代开始前必读。定义开发流程、版本管理、快速查找、AI 协作规范、未来扩展方向。

## 一、开发流程（铁律，每次迭代必须走完）

```
1. 定位    用户反馈问题 → 查 04 错误对照表（是否已修/已知根因）
2. 对比    查 01 来源归档 + git log：问题首次引入版本 → 对比上一版本
           连接行为参照 v2.9.8，严重问题先查 04 错误对照表 B38-41，原版行为参照 upstream-try/master
3. 澄清    确认触发条件、影响范围、是否与已知决策冲突（decisions skill）
4. 规划    整体考虑：连接机制 + 缓存机制 + 拼装机制 + 收尾链路，禁止只改一段
5. 修改    按 02 模块地图标注的约束修改，请求体零改动原则
6. 验证    本地编译 + 逻辑自查（改了什么/影响什么/边界条件）
7. 推送    用户明确授权 → push → CI 绿 → 提供 artifact 直链
8. 记录    03 修改全记录 + changelog skill + bug-record skill 同步更新
```

## 二、版本管理

- 版本号：前两位锁定，只能递增第三位（v3.5.x）
- versionCode 与版本对应（v3.5.17 → 持续递增）
- 所有查询到的版本控制做好：哪个版本改了什么、修了什么、发生了什么
- 推送清单：改 → 本地验证 → 用户确认 → push → CI 绿 → artifact 直链（7 天保留）
- 推送 ≠ 交付 APK，用户只能测试 APK

## 二·五、新问题排查法（历史无关联的莫名 bug）

很多 bug 是新的、莫名其妙的。按此方法最小成本爬遍可能相关的代码：

1. 现象归类：把现象映射到排查起点（问题排查索引，见记忆与 02 模块地图）
2. 查历史关联：四维速查表（04a）确认是否出现过、首现版本、当时修改
3. 若有历史关联：按当时修改方向验证是否回归（git diff 对比当前与修复版本）
4. 若无关联（新问题）：
   a. git log 该文件全历史，看最近几次改动（引入嫌疑）
   b. git diff 相邻版本，确认当前版本引入了什么
   c. 沿文件连接地图上下游爬：入口文件 → 中间链路 → 出口文件，逐个检查数据流
   d. 关键路径加 TraceLogger 日志，用运行日志定位断点
   e. 对照原版/2.x 行为（01 来源归档），差异即嫌疑
5. 判断准则：找出可验证的因果链才算定位，禁止猜测性修复
6. 定位后：走完整修改流程 + 联动更新记录

## 三、快速查找索引

| 想做什么 | 查哪里 |
|----------|--------|
| 任何问题排查 | 04a 四维速查表（首现版本/当时修改/回滚参照） |
| 定位代码文件 | 02a 全文件索引（105 包） |
| 需要回滚 | 04b 回滚参照矩阵（按域） |
| 修连接问题 | 04 B18/B21/B22 + 02 传输层 + 03 v3.5.17 |
| 修缓存问题 | 04 B14 + decisions D2 + 03 v3.5.11 |
| 修 MCP 问题 | 04 B19 + 02 MCP 链路 |
| 修收尾问题 | 04 B20 + 02 ChatService |
| 加新工具 | 02 工具域链路 + invoke_tools 入口 |
| 对比原版 | 01 来源归档 + upstream-try/master |
| 了解历史 | 03 修改全记录 + changelog skill |
| 方案决策 | decisions skill |
| 下一步计划 | roadmap skill |

## 三·五、修改联动表（改了什么必须跟着改什么，强制）

| 改了哪里 | 必须跟着做 | 原因 |
|----------|-----------|------|
| ChatCompletionsAPI | 同步检查 ResponseAPI | 两文件行为禁止漂移 |
| 请求体（system/tools/消息结构） | 评估缓存影响并写入提交说明 | 请求体零改动原则 |
| 连接配置（DataSourceModule） | 参照 v2.9.8；protocols 禁止 HTTP/2 | 连接稳定铁律 |
| ChatService 收尾 | 保证 onCompletion 用 NonCancellable | 取消态收尾完整 |
| McpManager getTransport | 必须在 runCatching 内 | 防状态撕裂 |
| 任何 bug 修复 | 更新 bug-record skill + 04 错误对照表 | 防重复诊断 |
| 任何版本发布 | 更新 changelog skill + 03 修改全记录 | 版本控制强制 |
| 任何方案决策 | 更新 decisions skill | 防重复论证 |
| 版本号变更 | 同步递增 versionCode | 版本管理规范 |
| 推送完成 | 提供 CI artifact 直链 | 用户只能测 APK |

## 四、AI 协作规范（高度适配 AI 修改）

1. 修改前必须加载：project-brief（架构基线）+ bug-record（历史根因）+ 本框架
2. 修改必须标注：文件、函数、改动原因、影响范围（在 commit message 和代码注释）
3. 每个关键决策写入 decisions skill，防重复论证
4. 每次修复写入 bug-record，防重复诊断
5. 禁止猜测：无证据不推进，先查文档/代码/日志
6. 禁止静默：中断必须可见，失败必须报错
7. 一次只解决一个根因，禁止顺手改无关代码
8. 改完必须自查边界：中断、取消、超时、空值、并发

## 五、模块化原则

- ai 模块：传输层纯逻辑，不依赖 Android 环境，可独立测试
- app/data：数据与业务逻辑分离
- app/ui：状态驱动，不直接操作数据层
- 服务层：编排逻辑集中，收尾必须 NonCancellable
- 新增功能优先放独立包（如 workflow/ecosystem/subagent），不塞进既有大文件

## 六、未来扩展方向

1. 传输层 G1-G4 缺口清理（G1 BEFORE_SYSTEM_PROMPT 为 P0）
2. 响应缓存适配：跨 Provider 统一缓存键管理
3. 工具体系：MCP 动态发现、技能包管理
4. 自动化测试：传输层单元测试（模拟 SSE 流）、集成测试
5. 原版上游更新跟随（见 06 原版兼容）
6. 多设备同步、Web 管理增强
7. AI 迭代自动化：问题→修复→推送→记录全链路工具化

## 七、已知约束（勿违反）

- 不覆盖本地文件，移植需手动适配架构
- 不改变落盘（持久化）机制
- 能用原版就用原版，在原版基础上做适配
- 测试环境固定：DeepSeek V4 Flash + Chat Completions API 流式
- 用户测试环境设备侧权限已全部满足，不接受以用户侧原因搁置
